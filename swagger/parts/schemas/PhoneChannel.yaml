type: object
properties:
  identification:
    $ref: ./PhoneChannelIdentification.yaml
  services:
    type: array
    items:
      $ref: ./PhoneChannelService.yaml
    minItems: 1
    maxItems: 20
    description: Traz a relação de serviços disponbilizados pelo Canal de Atendimento
required:
  - identification
  - services
