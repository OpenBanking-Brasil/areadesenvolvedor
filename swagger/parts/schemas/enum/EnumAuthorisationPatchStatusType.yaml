type: string
maxLength: 22
enum:
  - REVOKED
example: REVOKED
description: |
  Estado para o qual deve ir.

  [Restrição] O único valor válido é REVOKED. (caso contrário a detentora deve retornar erro HTTP 400)
