type: object
description: Objeto que reúne informações de tarifas de serviços
properties:
  services:
    type: array
    items:
      $ref: ./FinancingService.yaml
    minItems: 1
required:
  - services
