type: object
required:
  - data
  - links
  - meta
properties:
  data:
    type: object
    required:
      - status
    properties:
      status:
        type: array
        items:
          $ref: ./Status.yaml
  links:
    $ref: ./Links.yaml
  meta:
    $ref: ./Meta.yaml
