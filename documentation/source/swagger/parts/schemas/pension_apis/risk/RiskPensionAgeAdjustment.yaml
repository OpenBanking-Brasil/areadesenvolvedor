type: object
required:
  - criteria
  - frequency
properties:
  criteria:
    type: array
    items:
      $ref: ../../enum/EnumInsurancePersonalAgeAdjustmentCriteria.yaml
  frequency:
    type: integer
    format: int32
    description: Período em anos, caso critério de reenquadramento após ou a cada período em anos.
    maxLength: 3
    example: 10
additionalProperties: false
