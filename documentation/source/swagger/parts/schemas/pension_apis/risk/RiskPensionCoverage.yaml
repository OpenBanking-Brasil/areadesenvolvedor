type: object
required:
  - type
  - attributes
properties:
  type:
    $ref: ../../enum/EnumRiskPensionCoverageType.yaml
  additionalInfo:
    type: array
    items:
      type: string
    description: Lista a ser preenchida pelas participantes quando houver 'Outras' (permitir a publicação de uma nova cobertura que não esteja na lista básica).
  attributes:
    $ref: ./RiskPensionCoverageAttributes.yaml
  coveragePeriod:
    $ref: ../../enum/EnumRiskPensionCoveragePeriod.yaml
additionalProperties: false

