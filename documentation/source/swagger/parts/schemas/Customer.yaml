type: object
required:
  - rate
properties:
  rate:
    type: string
    description: |
      Percentual de clientes em cada faixa.
    pattern: (^[0-9](\.[0-9]{4})$|^NA$)
    example: "0.1500"
    maxLength: 6
