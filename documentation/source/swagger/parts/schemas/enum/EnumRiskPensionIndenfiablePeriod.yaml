type: string
description: |
  Listagem do pagamento para cada benefício:
    1. Prazo;
    2. Até o fim de ciclo determinado.
enum:
  - PRAZO
  - ATE_FIM_CICLO_DETERMINADO
maxLength: 200
example: PRAZO