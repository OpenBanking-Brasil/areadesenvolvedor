type: string
description: |
  A considerar os domínios abaixo:
    1. Pagamento Único;
    2. Parcelado;
    3. Ambos (Pagamento único e Parcelado)
maxLength: 15
enum:
  - PAGAMENTO_UNICO
  - PARCELADO
example: PAGAMENTO_UNICO
