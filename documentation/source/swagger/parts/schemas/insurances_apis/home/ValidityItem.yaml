type: object
required:
  - term
properties:
  term:
    $ref: ../../enum/EnumInsuranceTermType.yaml
  termAdditionalInfo:
    type: string
    description: Texto livre para complementar informação relativa ao term, quando for selecionada a opção 'Outros'
    maxLength: 255
additionalProperties: false
