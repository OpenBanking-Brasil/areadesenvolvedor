type: object
required:
  - amount
  - unit
properties:
  amount:
    type: string
    pattern: '^\d{1,16}\.\d{2,4}$'
    description: Valor da franquia em reais para cada combinação de modalidade/cobertura do produto.
    maxLength: 21
    example: '50.000'
  currency:
    $ref: ../../business_commons/CurrencyCode.yaml
additionalProperties: false