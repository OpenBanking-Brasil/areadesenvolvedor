type: object
required:
  - contractBaseType
  - contractBaseMinValue
  - contractBaseMaxValue
properties:
  contractBaseType:
    type: array
    description: |
       Incidência ao capital segurado da cobertura de casco. A considerar os seguintes domínios abaixo:
        1. Valor determinado;
        2. Valor de Mercado (Tabela FIPE);
        3. Valor Determinado e/ou Valor de Mercado
    minItems: 1
    items:
      $ref: ../../enum/EnumCarContractBaseType.yaml
  contractBaseMinValue:
    $ref: ./ContractBaseValue.yaml
  contractBaseMaxValue:
    $ref: ./ContractBaseValue.yaml
additionalProperties: false
