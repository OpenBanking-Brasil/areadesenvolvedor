type: object
properties:
  name:
    $ref: ./enum/EnumBranchServicesNames.yaml
  code:
    $ref: ./enum/EnumBranchServicesCodes.yaml
  additionalInfo:
    type: string
    description: Texto livre para complementar informação relativa ao Serviço disponível, quando for selecionada a opção 'OUTROS_PRODUTOS_SERVICOS'
    maxLength: 2000
    pattern: \w*\W*
required:
  - code
  - name
