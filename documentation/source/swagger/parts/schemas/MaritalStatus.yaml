type: object
description: Objeto agrupador das informações relativas ao estado civil da Pessoa Natural
required: 
  - code
properties:
  code:
    $ref: ./enum/EnumMaritalStatusCode.yaml
  additionalInfo:
    type: string
    description: Campo livre para complementar a informação relativa ao estado civil, quando selecionada a opção `OUTROS`
    pattern: \w*\W*
    maxLength: 50
example:
  code: 'SOLTEIRO'
