type: object
required:
  - name
  - code
properties:
  name:
    $ref: ./enum/EnumElectronicChannelsServicesName.yaml
  code:
    $ref: ./enum/EnumElectronicChannelsServicesCode.yaml
  additionalInfo:
    type: string
    maxLength: 2000
    pattern: \w*\W*
    description: Texto livre para complementar informação relativa ao Serviço disponível, quando for selecionada a opção 'OUTROS_PRODUTOS_SERVICOS'
    example: "SIC"
